@model Gamefacers.Models.GroupIndexViewModels

<div class="right-column">
    <fieldset>
        <legend> Members</legend>
        @foreach (var member in Model.Members)
                {
       
                    <p>@member.FullName </p>
                }
        
         
    </fieldset>
</div>

<div class="left-column">
    <fieldset>
        <legend> Description </legend>
        <p> @ViewBag.desc </p>
    </fieldset>
    <br />

    @Html.ActionLink("Join", "Join", "Group", new { id = ViewBag.Id }, null)
</div>


<div>
    <fieldset>
        <img src="@ViewBag.photo" class="banner-pic" />
    </fieldset>
</div>

@using (Html.BeginForm("PostStatus", "Group", new { id = ViewBag.Id }, FormMethod.Post, null))
{ 
    @Html.AntiForgeryToken()
    <div class="main-column">

        <div class="form-group">
            @Html.LabelFor(model => model.Status.StatusText, htmlAttributes: new {@class = "control-label col-md-2"})
            <div class="col-md-10">
                @Html.EditorFor(model => model.Status.StatusText, new {htmlAttributes = new {@class = "status-textarea"}})
                @Html.ValidationMessageFor(model => model.Status.StatusText, "", new {@class = "status-textarea"})
                <input class="status-button" name="Post" type="submit" value="PostStatus" />
            </div>
        </div>
     </div>
}


            <br/>
            <br/>
        <div class="main-column">
           
                @foreach (var status in Model.Statuses)
                {
                    <fieldset class="status">
                        <!-- Hér kemur statusinn-->
                        <p> @status.StatusText </p>

                        <textarea class="comment" placeholder="comment on status"></textarea>
                        <input class="status-button" type="submit" value="post"/>
                        <!--Hér kemur comment og post-takki-->
                    </fieldset>
                    <br/>
                    <br/>
                }
            

        </div>
