@model IEnumerable<Gamefacers.Models.Group>

@{
    ViewBag.Title = "Other";
}

<h2>Other</h2>

<form>
    <fieldset>
        <legend>Other</legend>
        @foreach (var item in Model)
        {
            <p>
                @item.GroupName
            </p>
            <p>
                @item.PhotoUrl
            </p>
        }
    </fieldset>
</form>